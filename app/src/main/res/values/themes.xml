<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HatGiongTamHon" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Áp dụng bảng màu đã định nghĩa -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>

        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_container</item>

        <item name="android:windowBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>

        <item name="colorSurfaceContainer">@color/primary_container</item>
        <item name="colorSurfaceVariant">@color/primary_container</item>


        <item name="colorOutline">@color/outline</item>
        <!-- Đổi màu thanh trạng thái (Status Bar) đồng nhất với app -->
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/main_font</item>
    </style>

    <style name="Theme.HatGiongTamHon" parent="Base.Theme.HatGiongTamHon" />
</resources>